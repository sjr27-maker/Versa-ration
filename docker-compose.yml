services:
  postgres:
    image: postgres:16
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: probe
      POSTGRES_PASSWORD: probe
      POSTGRES_DB: probe
    volumes:
      - probe_pgdata:/var/lib/postgresql/data
volumes:
  probe_pgdata: